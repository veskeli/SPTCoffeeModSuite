<UserControl x:Class="SPTCoffeeModManager.WindowHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="31">
    <Border Background="{DynamicResource BrushSurface1}" Height="31" MouseDown="Header_MouseDown" Margin="0,0,0,0">
        <Grid>
            <TextBlock Text="{Binding HeaderText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="16"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource BrushTextPrimary}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                <Button Content="—" Width="30" Margin="0,0,5,0"
                        Click="MinimizeButton_Click"
                        Background="Transparent" Foreground="{DynamicResource BrushTextPrimary}" BorderBrush="Transparent" />
                <Button Content="✕" Width="30"
                        Click="CloseButton_Click"
                        Background="Transparent" Foreground="{DynamicResource BrushTextPrimary}" BorderBrush="Transparent" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>