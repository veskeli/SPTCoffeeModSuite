<Window x:Class="SPTCoffeeModManager.ModProfileManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SPTCoffeeModManager"
        mc:Ignorable="d"
        Title="Mod Profiles"
        Width="760"
        Height="460"
        MinWidth="700"
        MinHeight="420"
        ResizeMode="CanResize"
        Background="{DynamicResource BrushAppBackground}"
        Foreground="{DynamicResource BrushTextPrimary}"
        WindowStyle="None"
        Padding="0">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CaptionHeight="0" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <local:WindowHeader Grid.Row="0" HeaderText="Mod Profiles" />

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource BrushSurface1}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" CornerRadius="8" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ListBox Name="ProfilesListBox"
                             ItemsSource="{Binding Profiles}"
                             SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                             Background="{DynamicResource BrushAppBackground}"
                             BorderBrush="{DynamicResource BrushBorder}"
                             Margin="0,0,0,6">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="4,2">
                                    <TextBlock Text="(active)"
                                               Foreground="{DynamicResource BrushAccentAlt}"
                                               FontSize="11"
                                               Margin="8,0,0,0"
                                               DockPanel.Dock="Right"
                                               Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Name="NewProfileButton" Content="New" Width="70" Margin="0,0,8,0" Style="{StaticResource ButtonSecondaryStyle}" Click="NewProfileButton_Click" />
                        <Button Name="DuplicateProfileButton" Content="Duplicate" Width="85" Margin="0,0,8,0" Style="{StaticResource ButtonSecondaryStyle}" Click="DuplicateProfileButton_Click" IsEnabled="{Binding IsProfileSelected}" />
                        <Button Name="DeleteProfileButton" Content="Delete" Width="70" Style="{StaticResource ButtonDangerStyle}" Click="DeleteProfileButton_Click" IsEnabled="{Binding IsDeleteButtonEnabled}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource BrushSurface1}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" CornerRadius="8" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Profile settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,12" />

                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Profile Name" />
                        <TextBox Grid.Column="1" Name="ProfileNameTextBox" Text="{Binding SelectedProfile.Name, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsProfileSelected}" />
                    </Grid>

                    <Grid Grid.Row="2" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Is Server Profile" />
                        <CheckBox Grid.Column="1" IsChecked="{Binding SelectedProfile.IsServerProfile}" IsEnabled="{Binding IsProfileSelected}" />
                    </Grid>

                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Profile Info" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Vertical">
                            <TextBlock VerticalAlignment="Center" Text="{Binding SelectedProfileModsText}" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding SelectedProfileConfigsText}" Margin="0,4,0,0" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Name="ActivateProfileButton" Content="Set Active" Width="100" Margin="0,0,8,0" Style="{StaticResource ButtonAccentStyle}" Click="ActivateProfileButton_Click" />
                        <Button Name="SaveProfileButton" Content="Save" Width="80" Margin="0,0,8,0" Style="{StaticResource ButtonSecondaryStyle}" Click="SaveProfileButton_Click" />
                        <Button Name="CloseButton" Content="Close" Width="80" Click="CloseButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>


